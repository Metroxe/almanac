# Local dev only — the published MCP reads ALMANAC_BACKEND_URL from the
# coding-agent's MCP config (Cursor / Claude Code / Cline), not from a file.
# Defaults to https://api.almanac.dev when unset.
ALMANAC_BACKEND_URL=http://localhost:8787
